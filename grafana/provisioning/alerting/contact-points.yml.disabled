apiVersion: 1
contactPoints:
  - orgId: 1
    name: default-email
    receivers:
      - uid: email
        type: email
        settings:
          addresses: "${ALERTS_EMAIL_TO}"
        secureJsonData:
          authPassword: "${ALERTS_EMAIL_PASS}"
        disableResolveMessage: false
